<h1 class="text-center mt-4"><%= @user.username %>'s details</h1>

<div class="text-center mt-4">
  <%= link_to "Edit your profile", edit_user_path(@user), class: "btn btn-outline-info" %>
</div>

<%= render 'groups/group' %>
